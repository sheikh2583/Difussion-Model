# Environments
venv/
env/
.env

# Python Caches
__pycache__/
*.pyc
*.pyo
*.pyd

# IDEs
.vscode/
.idea/
*.swp

# Build and Distribution
dist/
build/
*.egg-info/
tmp/

# Large Files & Directories (Local Data/Outputs)
# Keep Python packages under data/ and models/ tracked; ignore only artifacts.
data/raw/
# Generated latent tensors and downloaded model weights stay local. Keep the
# small config/source manifests under data/pretrained/ eligible for tracking.
data/latent_cache/
data/pretrained/**/*.bin
data/pretrained/**/README.md
*.pt
*.pth
*.ckpt
*.safetensors
data/reflow_pairs_*.pt
data/history/
models/checkpoints/
results/
*.pdf
*.pptx
*.zip
!thesis_context.zip
*.gif
thesis_dashboard.html
ProjectForClaude.zip

# Deliberately versioned, generated thesis handoff artifacts.
!THESIS_SUMMARY.md

# Training transcripts and their metadata are experiment provenance. Keep the
# device/representation/algorithm partitions eligible for version control.
!training_logs/
!training_logs/**

# Agent conversation files
AGENTS.md
PLAN.md
CROSS_TRACK.md
*_PLAN.md
BLOCKER_DECISIONS.md
